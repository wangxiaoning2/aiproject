# ai-week1-streaming

手写 SSE 的流式对话 demo。**零运行时依赖**，不装任何库，也没有构建步骤。

目的只有一个：让"大模型流式输出"从黑盒变成你能逐行读懂的东西。跑通它，你就跨过了第 1 周最大的那道坎——把 AI 从"好神奇"降级成"一个慢一点、按流返回、可能中途挂掉的接口"。

---

## 跑起来（两条命令）

```bash
cd ai-week1-streaming
node rag/ingest.ts     # 建知识库索引（只跑一次；data/ 里的笔记变了要重跑）
node server.ts
```

打开 http://localhost:5173 就能用。没配 API Key 会自动进 **mock 模式**——本地伪造流式输出，不花钱，但走的链路和真实模型一模一样。

想看真实模型，就这样：

```bash
cp .env.example .env
# 编辑 .env，填上 LLM_API_KEY，LLM_BASE_URL 换成你要用的服务商
node --env-file=.env server.ts
```

任何 OpenAI 兼容端点都能接：DeepSeek、通义、智谱、Kimi、本地 Ollama 都行。

---

## 五个必须搞懂的机制

这五个就是这个 demo 的全部内容，也是第 1 周任务清单上的五件事。

### 1. 为什么不能直接调模型，要自己写一个 Node 代理？

因为 **API Key 不能出现在浏览器里**。一旦写进前端 JS，任何人打开 DevTools 就能拿走你的额度。

所以浏览器 → 你的服务端 → 模型服务商。中间这一层代理顺便帮你做了三件事：藏密钥、裁剪上下文、把成本算出来。

### 2. SSE 到底长什么样

它就是一段**纯文本**，通过一条不关闭的 HTTP 连接持续推过来。格式简单到离谱：

```
event: token
data: {"t":"你"}

event: token
data: {"t":"好"}

event: done
data: {"usage":{...}}
```

规则只有三条：

- 一行 `event:` 声明事件名
- 一行或多行 `data:` 承载数据
- **一个空行表示这一帧结束**

服务端按这个格式写（见 `server.ts` 的 `sse()` 函数），前端按这个格式读。就这么简单。

> 为什么不用 `EventSource`？因为它只能发 GET，没法带 JSON body、没法自定义请求头。所以真实项目里几乎都是 `fetch` + `ReadableStream` 自己手动解析——就是 `public/app.js` 里 `handleFrame` 干的事。

### 3. 流式解析最容易踩的坑：chunk ≠ 一帧

这是写流式最容易翻车的地方。

你 `await reader.read()` 拿到的 `chunk`，**不代表一条完整的 SSE 消息**。TCP 只是字节流，服务端一次写入的内容可能被拆成两个 chunk 送过来，变成这样：

```
第一次 read():  "event: token\ndata: {\"t\":\"你"
第二次 read():  "好\"}\n\n"
```

如果你假设 chunk 等于一帧，就会在某些请求里随机丢字——而且只在网络抖动时出现，极难复现。

正确做法就一句：**自己维护 buffer，累积解码后的字符串，按空行切分，剩下的留在 buffer 里等下一轮**。

`public/app.js` 里那段 `buffer += decoder.decode(value, { stream: true })` 就是干这个的。注意 `{ stream: true }` 不能省——中文一个字占 3 个字节，正好被切在两半的时候，不加这个参数会解出乱码。

### 4. 用户点"停止"，钱该什么时候停

很多人以为前端不渲染就是停止生成了。不是的。

前端停止读流，**服务端和模型之间的请求还在跑**，token 还在烧钱，账单照记。你必须让这件事传到上游去：

- 前端：`AbortController.abort()`
- 服务端：监听 `res.on('close')`，一收到就 `controller.abort()` 掐掉上游的 fetch

`server.ts` 里这段是有意写给你看的：

```ts
res.on("close", () => {
  if (!controller.signal.aborted) {
    controller.abort();
    console.log(`[abort] 客户端断开，已取消上游请求`);
  }
});
```

试一下：**回答到一半点停止生成，然后看终端日志**。

### 5. 上下文不是无限的，也不是免费的

模型本身没有记忆。你看到的"它记得前面说过什么"，其实是每次请求都把整个对话历史重新发一遍。

这带来两个必须处理的问题：

- **上下文窗口会满。** `server.ts` 里的 `trimContext()` 是最朴素的策略：从最新往前保留，超预算的老消息直接丢。生产环境会做得更细——摘要压缩、只保留相关轮次、把长期信息落到向量库。
- **每次都要为全部历史付费。** 对话到第 20 轮的时候，输入 token 会远超输出。所以成本优化的大头在输入端，不在输出端。

顺带理解一下 `estimateTokens()`：中文一个字大约 1 token，英文 4 个字符大约 1 token。这个估算不精确，但足够用来做预算控制。

---

## 里程碑 1：给这个 demo 长出知识库（RAG）

同一个仓库往上迭代，不另起一个项目。面试官会深挖一个项目二十分钟，不会把三个项目各挖五分钟。

### RAG 就两件事

| 阶段 | 什么时候跑 | 干什么 |
|---|---|---|
| 离线（`rag/ingest.ts`） | 只跑一次，笔记变了才重跑 | 读 `data/` 里的笔记 → 切块 → 算向量 → 存成 `rag/index.json` |
| 在线（`server.ts`） | 每次提问 | 把问题也算成向量 → 找最像的几段 → 拼进 system prompt → 照常流式回答 |

**没有一行数学，全是工程。** 唯一的新东西是 Embedding（把文字压成一串数字），而它也是调接口，不是自己算。

### 怎么用

1. 把 `data/` 里那三篇示例笔记换成你自己的（Markdown / txt 都行，可以放子目录）
2. `node rag/ingest.ts` 重建索引
3. 打开页面上「知识库」开关，正常提问
4. 想只看检索、不调模型：点「只检索」——**不花一分钱**，调 RAG 时用得最多的就是这一步

命令行也能查：

```bash
node rag/query.ts "怎么防止账单爆炸" 4
```

### 三种问法，对比着试（都用 mock 就能看出差别）

| 问题 | 结果 | 为什么 |
|---|---|---|
| 怎么知道改好了 | 相似度 0.34，命中 `rag-basics.md` 的同名小节 | 用词和笔记一致，字面检索最强的地方 |
| 模型会不会瞎编 | 命中跑到别的段落去了 | 「瞎编」和笔记里的「会编」字面对不上 |
| 它会不会乱说话 | 相似度 0.01，基本等于没捞到 | 意思一样，但一个字都没重合 |

最后两种失败**不是 bug**，是字面检索的天花板。真实项目里靠的是语义嵌入模型（问「乱说话」也能命中「会编」），这就是为什么 `.env.example` 里那几行 `EMBED_*` 值得你配一次真实的。

> mock 用的向量是本地 IDF 加权的 n-gram 哈希，本质是"带权重的关键词匹配"，不是语义。它的用途只有一个：让你在没花一分钱的情况下，把 RAG 的每一步都跑通、看明白、能改。

### 三个练习（做完你就真懂 RAG 了）

**练习 A · 把块切小。** 打开 `rag/chunk.ts`，把 `targetSize` 从 320 改成 100，重跑 `node rag/ingest.ts`，再问同样的问题。看召回是不是变碎了、答案是不是开始断章取义。**这是 RAG 最核心的权衡，亲手感受一次胜过读十篇。**

**练习 B · 把 topK 从 4 改成 1。** 页面上直接拖那个滑块。召回变少 → 提示词变短 → 输入 token 变少 → 便宜了，但答案开始漏关键信息。成本和效果的边界就在这个旋钮上。

**练习 C · 让它答不该答的。** 问一个笔记里根本没写的问题（比如「数据仓库怎么分层」）。看它是不是老实说"笔记里没有相关记录"。**如果不老实，就去改 `server.ts` 里的 `buildRagSystemPrompt()`**——那里就是防编造的战场。

### 代码地图（RAG 部分）

```
rag/types.ts     每一步流向下一步的数据长什么样
rag/chunk.ts     切块：按标题分节 + 段落聚合 + 相邻块重叠
rag/embed.ts     嵌入：本地 IDF 伪向量 / 真实 OpenAI 兼容接口
rag/store.ts     存与查：JSON 当向量库 + 余弦相似度
rag/ingest.ts    离线入口：读文件 → 切块 → 嵌入 → 落盘（命令行 + 可被服务端调用）
rag/query.ts     命令行检索，调 RAG 的主力工具
data/            你的笔记。现在是三篇示例，换成自己的就是你的项目
```

`server.ts` 里新加的三块：

| 位置 | 看什么 |
|---|---|
| `retrieve()` | 在线检索的全部逻辑，只有十几行 |
| `buildRagSystemPrompt()` | 怎么把召回的段落拼成"小抄"。面试必问的那几句约束就在这 |
| `handleRagSearch()` | `/api/rag/search`：只检索不生成，调试 RAG 的第一入口 |

---

## 三个动手练习（照着做一遍，比读十篇文章管用）

**练习一 · 打开"原始 SSE 报文"面板**
发一句话，然后盯着那个面板看。你会发现所谓"AI 打字"，本质就是一堆小 JSON 拼在一起，一个 token 一个 `data:` 帧。黑盒感在这一刻就消失了。

**练习二 · 发「测试报错」**
mock 模式里埋了一个故障开关：收到这句会在流到一半时抛错。观察前端怎么处理——自动重试一次，第二次还失败才把错误暴露给用户。然后回答自己一个问题：**为什么失败的回答不能塞进上下文？**（因为那样模型会以为自己真的说过那些话。）

**练习三 · 压低 MAX_CONTEXT_TOKENS**
`.env` 里把它改成 `200`，重启，然后连着聊几轮。看右侧"上下文条数"面板里 `（−N）` 是怎么涨起来的——那是被裁掉的消息数。你就直观地看到了"上下文预算"这件事。

---

## 代码地图

```
server.ts              服务端：静态文件 + /api/chat 流式代理 + /api/rag/* 检索接口（Node 22 直接跑 TS）
public/index.html      页面结构
public/app.js          前端：手写 SSE 解析 + rAF 攒批渲染 + 中断/重试 + 召回面板
rag/                   知识库：切块 / 嵌入 / 检索 / 建索引（里程碑 1 加进来的）
data/                  你的笔记，索引的数据源
.env.example           配置模板
```

几处值得反复看的代码：

| 位置 | 看什么 |
|---|---|
| `server.ts` → `sse()` | 一个 SSE 帧怎么组装 |
| `server.ts` → `streamUpstream()` | 怎么手动解析上游的流，包括半截 JSON 怎么处理 |
| `server.ts` → `trimContext()` | 上下文裁剪的最朴素实现 |
| `public/app.js` → `runStream()` 内的 `for(;;)` | chunk 粘包的正确处理方式 |
| `public/app.js` → `pushToken()` | 为什么要用 requestAnimationFrame 攒批 |

---

## 下一步可以自己加的功能

按难度排的，做完第一个你就有了一个能写进简历的项目：

1. **多会话管理** —— 左侧会话列表，切换时恢复历史
2. **Markdown 流式渲染** —— 注意流式过程中不能整段重渲染，得处理"半个代码块"的场景
3. **token 逐字淡入动画** —— 纯前端体验题，你的主场
4. **真实 usage 面板** —— 打开 `LLM_INCLUDE_USAGE=1`，对比估算值和真实值差多少
5. **换个后端语言重写一遍** —— 用 Python + FastAPI 写同样的东西，这就是第 3–6 周的 Python 练习
6. **给召回加"出处高亮"** —— 回答里标了【资料 N】，点一下能跳到左边对应的召回卡片。这是纯前端活，也是最能打动面试官的体验细节

---

## 已知限制

这是教学项目，不是生产代码。缺的东西：没有鉴权、没有限流、没有并发控制、没有真正的持久化、上下文裁剪策略很粗糙、日志只打到终端。

生产环境还需要：请求队列与并发上限、按用户的额度控制、SSE 的断线重连与断点续传、敏感词与内容安全、全链路追踪。

——这些缺口本身就是你后面几周要补上的东西，也是面试里可以主动聊的加分点。
